export const getLayout = (ids: string[], mode: '1' | '2', ext?: { fullId: string }) => {
  switch (mode) {
    case '1':
      {
      }
      break
    case '2':
      {
        const { fullId } = ext
      }
      break
  }
}
